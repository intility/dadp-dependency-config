{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Automerge disabled for all updates",
  "automerge": false,
  "platformAutomerge": false,
  "packageRules": [
    {
      "description": "Security patches - immediate schedule but still require review",
      "isVulnerabilityAlert": true,
      "schedule": ["at any time"],
      "addLabels": ["security"],
      "prPriority": 10
    }
  ]
}
